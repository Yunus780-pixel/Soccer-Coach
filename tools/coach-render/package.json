{
  "name": "coach-render",
  "private": true,
  "type": "module",
  "description": "Offline tool: records the 3D coach doing each drill (from Front/Side/Back) into looping WebM videos that the app plays. Not part of the app build or the pnpm workspace.",
  "scripts": {
    "render": "node render.mjs"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "puppeteer-core": "^23.0.0"
  }
}
